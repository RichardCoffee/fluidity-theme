<?php

if (!defined('TCC_USE_GALLERYVIEW')) { define('TCC_USE_GALLERYVIEW',false); }
/*
$defines = array('TCC_USE_GALLERYVIEW' => false);

foreach($defines as $define=>$value) {
	if (!defined($define)) { define($define,$value); }
}

unset $defines; //*/
